import { websiteDataAccessPost } from './website-data-access-post';

describe('websiteDataAccessPost', () => {
  it('should work', () => {
    expect(websiteDataAccessPost()).toEqual('website-data-access-post');
  });
});
