import { websiteDataAccessImage } from './website-data-access-image';

describe('websiteDataAccessImage', () => {
  it('should work', () => {
    expect(websiteDataAccessImage()).toEqual('website-data-access-image');
  });
});
