import { sharedTypesEnvironmentEnvironment } from './shared-types-environment-environment';

describe('sharedTypesEnvironmentEnvironment', () => {
  it('should work', () => {
    expect(sharedTypesEnvironmentEnvironment()).toEqual(
      'shared-types-environment-environment'
    );
  });
});
