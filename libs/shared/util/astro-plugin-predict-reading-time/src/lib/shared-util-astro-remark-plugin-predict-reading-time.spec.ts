import { sharedUtilAstroRemarkPluginPredictReadingTime } from './shared-util-astro-remark-plugin-predict-reading-time';

describe('sharedUtilAstroRemarkPluginPredictReadingTime', () => {
  it('should work', () => {
    expect(sharedUtilAstroRemarkPluginPredictReadingTime()).toEqual(
      'shared-util-astro-remark-plugin-predict-reading-time'
    );
  });
});
