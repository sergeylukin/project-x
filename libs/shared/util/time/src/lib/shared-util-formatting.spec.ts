import { sharedUtilFormatting } from './shared-util-formatting';

describe('sharedUtilFormatting', () => {
  it('should work', () => {
    expect(sharedUtilFormatting()).toEqual('shared-util-formatting');
  });
});
