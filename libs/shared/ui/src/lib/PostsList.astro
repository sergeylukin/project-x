---
import { PostsListItem as Item } from '@sergeylukin/shared/ui';
import type { IPost } from '@sergeylukin/shared/entities/post';

export interface Props {
  posts: Array<IPost>;
}

const { posts } = Astro.props;
---

<ul>
  {
    posts.map((post) => (
      <li class="mb-12 md:mb-20">
        <Item post={post} />
      </li>
    ))
  }
</ul>
